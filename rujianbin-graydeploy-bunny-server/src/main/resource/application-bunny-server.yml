spring:
  application:
    name: rujianbin-graydeploy-bunny-server
server:
  port: 8001
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/
